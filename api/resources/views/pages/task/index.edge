@layout('layout.index')

@section('content')
  <div class="row mb-3">
    <div class="col d-flex align-items-center">
      <a href="{{ route('admin.tasks.store') }}" class="btn btn-danger m-r-20">
        <i class="fa fa-plus"></i>
          New task
      </a>
      @if(old('result'))
        <div class="alert alert-success mb-0" role="alert">
          Theme has succesfull {{ old('result') }}
        </div>
      @endif
    </div>
  </div>
  @include('partials.tasks.index')
@endsection