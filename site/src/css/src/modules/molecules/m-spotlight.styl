.m-spotlight {
  font-size: em(20);
  color: $primary;
  font-weight: $fontweight.bold;
  text-align: center;
  width: 100%;
  margin-bottom: em(40, 20);
  display: flex;
  flex-direction: column;
  align-items: center;
}

.m-spotlight_icon {
  max-width: em(90);
}