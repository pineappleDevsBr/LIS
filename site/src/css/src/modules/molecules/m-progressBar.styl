.m-progressBar {
  width: 100%;
  display: flex;
  align-items: center;

  &.rounded-borders {
    border-radius: 50% !important;
  }
}

.m-progressBar-value {
  font-size: em(18);

  &:first-child {
    margin-left: em(15);
  }

  &.-negative {
    color: $accent;
  }
}